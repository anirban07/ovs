#!/bin/bash

echo "$0 doing nothing"
# PROJECT_ROOT=$(pwd)
# BUILD_PATH=${PROJECT_ROOT}/build/docker/

# mkdir ${BUILD_PATH}

# rsync -a ${PROJECT_ROOT} ${BUILD_PATH}
# rsync ${PROJECT_ROOT}/support/docker/deploy/Dockerfile ${BUILD_PATH}
